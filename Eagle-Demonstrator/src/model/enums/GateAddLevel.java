package model.enums;

/**
 * The enum Gate add level.
 */
public enum GateAddLevel {
    /**
     * Must gate add level.
     */
    Must,
    /**
     * Can gate add level.
     */
    Can,
    /**
     * Next gate add level.
     */
    Next,
    /**
     * Request gate add level.
     */
    Request,
    /**
     * Always gate add level.
     */
    Always
}
